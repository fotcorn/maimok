#cloud-config
package_update: true
package_upgrade: true

users:
  - name: sysadmin
    ssh-authorized-keys:
      - ssh-rsa ...
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash

hostname: my-host-name
manage_etc_hosts: localhost
